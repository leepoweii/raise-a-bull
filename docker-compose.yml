services:
  callie:
    build: .
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file: .env
    volumes:
      - ./workspace:/app/workspace:ro
      - callie-data:/app/data

volumes:
  callie-data:
